#!/bin/bash
# UserPromptSubmit hook: light-touch session context
# Fires when user submits a prompt, adds context for Claude

echo "Pipeline project context: 3-tier ASR cleaning. Run pytest + --dry-run to verify changes. No static word lists."
exit 0
