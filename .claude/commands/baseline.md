---
description: "DEPRECATED — Use the skill at .claude/skills/baseline/SKILL.md instead. Invoke with /baseline"
---

This command has been replaced by the `/baseline` skill.

Use: `/baseline` or `/baseline <MatchName>`
