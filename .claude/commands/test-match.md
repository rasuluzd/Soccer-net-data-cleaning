---
description: "DEPRECATED — Use the skill at .claude/skills/test-match/SKILL.md instead. Invoke with /test-match"
---

This command has been replaced by the `/test-match` skill.

Use: `/test-match <MatchName> [--tier N]`
